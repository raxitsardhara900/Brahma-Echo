# Group 32: Dynamic iframe (inserted after load)

### 32.1 Wait for a late iframe, then interact with it
Navigate to `http://fixtures/iframe-dynamic.html`. An iframe appears after the page loads. Wait for it, then fill its input with "Late World" and submit. After submitting, read the result text inside the iframe to confirm it received the input.

**Verify**: Report the result text shown inside the iframe after submission.

---

