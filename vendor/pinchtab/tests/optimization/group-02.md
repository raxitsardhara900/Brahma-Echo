# Group 2: Search & Dynamic Interaction

### 2.1 Use wiki search to find a page
On `http://fixtures/wiki.html`, use the search form to search for "golang". Do not navigate directly -- use the search input on the page.

**Verify**: Which best describes the page you landed on?
- A) The URL contains "wiki-go" or the page is the Go article
- B) The search returned results but you stayed on the search page
- C) Other

### 2.2 Search with no results
On `http://fixtures/search.html`, search for "xyznonexistent" and report what happens.

**Verify**: The page handled the empty result gracefully without crashing.

### 2.3 Search for AI content
On `http://fixtures/search.html`, search for "artificial intelligence" and tell me what results come up.

**Verify**: Report includes the relevant search result(s).

---
