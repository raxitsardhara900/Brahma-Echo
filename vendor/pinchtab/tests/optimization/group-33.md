# Group 33: srcdoc iframe

### 33.1 Interact with an inline-content iframe
Navigate to `http://fixtures/iframe-srcdoc.html`. The page contains an iframe with inline content. Fill its input with "srcdoc" and submit. After submitting, read the result text inside the iframe to confirm it received the input.

**Verify**: Report the result text shown inside the iframe after submission.

---

